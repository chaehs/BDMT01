# Blueprint: 배드민턴 라켓 정보 및 후기 플랫폼 (BDMT01)

## 1. 프로젝트 개요

배드민턴 사용자들이 라켓 정보를 쉽게 검색하고, 실제 사용자들의 후기와 평점을 확인하며, 자신의 의견을 공유할 수 있는 웹 애플리케이션을 구축합니다.

---

## 2. 핵심 기능 및 UI/UX

### 2.1. USER 페이지

#### 2.1.1. 라켓 목록 (`RacketList.vue`)

- **초기 상태**: 초기 데이터 전송량 최소화를 위해 화면을 비워 둡니다. 필터나 검색을 통해 라켓 목록을 불러오도록 유도합니다.
- **필터링**:
    - 브랜드: 드롭다운 메뉴 (예: YONEX, VICTOR, LI-NING)
    - 무게: 드롭다운 메뉴 (예: 3U, 4U, 5U, 6U, 7U)
    - 밸런스: 드롭다운 메뉴 (HEAD LIGHT, EVEN BALANCE, HEAD HEAVY)
    - 탄성: 드롭다운 메뉴 (FLEXIBLE, MEDIUM, STIFF, EXTRA STIFF)
- **태그 검색**: 사용자는 인기 태그를 선택하거나 직접 검색하여 원하는 라켓을 찾을 수 있습니다.
- **라켓 목록**: `RacketCard` 컴포넌트들을 2열 그리드 형태로 표시하며, 화면 크기에 따라 반응형으로 조절됩니다.

#### 2.1.2. 라켓 카드 (`RacketCard.vue`)

- **디자인**: 신분증처럼 이미지와 정보가 배치된 사각형 카드 디자인입니다.
- **표시 정보**:
    - 브랜드(BRAND), 이름(NAME) - 영어는 대문자로 표기
    - **평점**: `★ 4.7(26)` 형식으로 표시 (1인 1평가 원칙).
    - **사용자 태그**: 가장 많이 등록된 상위 4개 태그를 노출하고, 나머지는 `+5`와 같이 숫자로 표시합니다. 태그는 최대 6자로 제한됩니다.
    - **핵심 스펙**: 무게(Weight), 밸런스(Balance) 등 주요 사양을 표시합니다.

#### 2.1.3. 라켓 상세 정보 (`RacketDetail.vue`)

- **활성화**: 라켓 카드를 클릭하면 상세 정보가 모달 창으로 열립니다.
- **콘텐츠**:
    - **평점 분포**: 5점부터 1점까지 각 별점에 몇 명이 참여했는지 막대그래프로 시각화하여 보여줍니다.
    - **전체 스펙**: `rackets` 테이블의 모든 정보를 상세히 표시합니다.
    - **태그 입력**: 사용자가 직접 태그를 추가할 수 있는 UI를 제공합니다. 기존 태그와 중복을 피하기 위해 자동 완성 기능을 구현하여 입력을 유도합니다.

#### 2.1.4. 프로필 페이지 (`Profile.vue`)

- **접근**: 로그인한 사용자가 헤더의 '프로필' 버튼을 클릭하여 접근합니다.
- **자동 리디렉션**: 로그인 직후 필수 프로필 정보(닉네임, 성별, 급수 등)가 입력되지 않은 경우, 이 페이지로 자동 리디렉션됩니다.
- **콘텐츠 및 기능**:
    - **표시 정보**: 이메일, 가입일
    - **입력 필드**:
        - **닉네임**: (텍스트 입력, 추후 중복 확인 기능 추가)
        - **성별**: 남(`M`), 여(`F`)
        - **급수**: S, AA, A, B, C, D, E, F, X
        - **사용 라켓**: (텍스트 입력)
        - **선호 무게**: 3U, 4U, 5U, 6U, 7U
        - **선호 밸런스**: Head Heavy(`hh`), Even Balance(`eb`), Head Light(`hl`)
        - **선호 포지션**: Front(`front`), Back(`back`), All(`all`)
    - **저장**: 사용자가 입력한 정보를 `profiles` 테이블에 저장하는 기능.

### 2.2. ADMIN 페이지 (`Dashboard.vue`)

- **목적**: 관리자가 라켓 데이터를 생성(Create), 조회(Read), 수정(Update), 삭제(Delete)할 수 있는 전용 페이지입니다.

### 2.3. 헤더 (App.vue)

- 로그인 시: 사용자 이메일, **프로필 버튼**, 로그아웃 버튼, (관리자인 경우) 관리자 버튼을 표시합니다.
- 로그아웃 시: 로그인 버튼을 표시합니다.

---

## 3. 기술 명세

### 3.1. 기술 스택

- **에디터**: 파이어베이스 스튜디오 (Firebase Studio)
- **프론트엔드**: Vue.js 3 (Composition API, `<script setup>`), JavaScript
- **백엔드 & DB**: Supabase
- **스타일링**: Tailwind CSS

### 3.2. 데이터베이스 스키마 (Supabase)

- **`rackets`**: 라켓 기본 정보
- **`tags`**: 공통 태그 마스터 테이블
- **`racket_tag_map`**: 라켓과 태그를 연결하는 매핑 테이블
- **`racket_reviews`**: 라켓 리뷰 및 평점 정보
- **`profiles`**: 사용자 프로필 정보
    - **신규 컬럼**: nickname, gender, level, main_racket, pref_weight, pref_balance, pref_position

### 3.3. 인증 (Authentication)

- **초기 방식**: Supabase Auth를 통한 구글 로그인
- **프로필 관리**: 신규 사용자 가입 시 트리거를 사용해 `profiles` 테이블에 자동으로 프로필 정보를 생성합니다.

### 3.4. 저장소 (Storage)

- **폴더**: `racket_storage`
- **파일명**: AI로 생성된 라켓 이미지를 `소문자_언더바` 형식으로 저장합니다.

### 3.5. 라우팅 (Routing)

- **`/`**: 사용자 페이지 (`RacketList.vue`)
- **`/admin`**: 관리자 대시보드 (`Dashboard.vue`)
- **`/login`**: 로그인 페이지 (`Login.vue`)
- **`/profile`**: 사용자 프로필 페이지 (`Profile.vue`)

---

## 4. 스타일링 및 배포

- **스타일링**: Tailwind CSS
- **배포**: GitHub 리포지토리를 Cloudflare와 연동하여 CI/CD를 구성합니다.

---

## 5. 개발 계획 및 이력

- **[✓] 컬럼명 변경**: `skill_level`을 `level`로 변경
- **[✓] 프로필 페이지 UI 수정**: 급수, 선호 포지션 필드 한글 제거
- **[✓] 프로필 페이지 기능 수정**: `appInfo.txt` 변경 사항(닉네임 필드 추가, 선호 무게 옵션 확장)을 프로필 페이지에 반영합니다.
- **[✓] 프로필 페이지 상세 기능 구현**: `appInfo.txt`에 명시된 상세 요구사항(성별, 급수, 선호 스펙 등)에 맞춰 프로필 입력 폼 및 저장 로직을 구현합니다.
- **[✓] 프로필 페이지 추가**: 로그인한 사용자를 위한 프로필 버튼 및 페이지 생성
- **[✓] 필터 기능 개선**: 대소문자 구분 없는 필터링 적용 (`ilike`)
- **[✓] 필터 UI 개선**: 필터 초기화 버튼 위치 조정
- **[✓] 초기 프로젝트 설정**: Vue 3 + Vite + JS
- **[✓] 컴포넌트 생성 및 UI 목업 구현**
- **[✓] 파일 구조 리팩토링**
- **[✓] Supabase 연동 및 초기 화면 로직 구현**
- **[✓] 필터링 기능 강화**
- **[✓] 기본 정보 관리 기능 (관리자)**
- **[✓] 사용자 인증 및 프로필 관리**
- **[보류] 고급 폼 로직**: 태그 및 색상 정보 저장

---

## 6. 현재 작업: 프로필 페이지 기능 구현

### 6.1. 목표

`appInfo.txt`에 명시된 요구사항에 따라 프로필 페이지(`Profile.vue`)의 UI와 기능을 완성합니다. 사용자가 자신의 프로필 정보(닉네임, 성별, 급수 등)를 입력하고 저장할 수 있도록 합니다.

### 6.2. 작업 단계

1.  **[✓] `blueprint.md` 업데이트**: 현재 작업 계획을 `blueprint.md`에 반영했습니다.
2.  **[✓] `Profile.vue` UI 및 기능 구현**: `appInfo.txt`에 명시된 모든 프로필 정보 필드(닉네임, 성별, 급수, 사용라켓, 선호무게, 선호밸런스, 선호포지션)를 포함하는 입력 폼을 생성하고, Supabase와 연동하여 데이터 조회 및 저장 기능을 구현합니다. (DB 저장 값 및 컬럼명 수정)
3.  **[ ] 리디렉션 로직 확인**: 프로필 정보가 없는 사용자가 다른 페이지 접근 시 `/profile`로 리디렉션되는 로직을 확인하고 필요시 보완합니다.
